# Synchronous R-Tree Traversal Spatial Join Algorithm

**High Level Overview**: Given two datasets A and B with R-Tree indices X and Y, use the R-Tree index structure to prune the search space.

**Implementation Difficulty**: Easy
