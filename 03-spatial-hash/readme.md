# Spatial Hash Join

**High Level Overview**: Given two datasets A and B, build n partitions for A and sort elements of B into partitions. Join elements in each partition.

**Implementation Difficulty**: Medium
